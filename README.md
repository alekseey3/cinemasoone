# CinemaSoon 🎬

**CinemaSoon** — учебное веб-приложение на платформе **Jakarta EE** для учёта ожидаемых фильмов.  
Пользователь может добавлять фильмы, редактировать и удалять записи, ставить лайки и смотреть топ самых ожидаемых премьер.

Проект реализован в рамках **практической работы №1** «Приложение с типовой архитектурой Jakarta EE».

---

## Функциональность

- добавление фильма с указанием:
    - названия;
    - предполагаемой даты выхода;
    - жанра (из списка);
    - описания / комментария;
- просмотр общего списка ожидаемых фильмов;
- редактирование информации о фильме;
- удаление фильма;
- лайки для фильмов (формирование раздела «Топ ожиданий»);
- просмотр фильмов по жанрам (через выпадающее меню «По жанрам» и страницу `genres.xhtml`);
- информационная страница «О проекте».

---

## Используемые технологии

**Платформа и стек:**

- **Jakarta EE**:
    - **JPA** — работа с БД через сущности (`User`, `Movie`);
    - **EJB (Stateless Session Bean)** — бизнес-логика (`MovieService`);
    - **JSF / Facelets** — слой представления (`MovieBean` + .xhtml-страницы).
- **Сервер приложений**: Eclipse GlassFish.
- **СУБД**: PostgreSQL.
- **Инструменты**: IntelliJ IDEA, pgAdmin, Git, GitHub.
- **Сборка проекта**: Maven.

---

## Архитектура

Приложение построено по классической **трёхслойной архитектуре**:

### 1. Слой данных (Entity, JPA)

- Сущность `User` → таблица `users`.
- Сущность `Movie` → таблица `movies`.

Основные поля `Movie`:

- `title` — название фильма;
- `releaseDate` — дата выхода;
- `genre` — жанр;
- `description` — описание;
- `createdAt` — дата создания записи;
- `likes` — количество лайков;
- `user` — связь с пользователем (`ManyToOne`).

SQL-скрипт для создания и первоначального заполнения БД лежит в файле:

```text
db/schema.sql
